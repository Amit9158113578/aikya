package com.idep.lifequote.util;


/**
 * 
 * @author yogesh.shisode
 *
 */
public class DocumentIDConfigLoad{
	/*static CBService serverConfig =  CBInstanceProvider.getServerConfigInstance();
	static JsonObject documentList = null;

	public static JsonObject getDocumentIDConfig(){
		if(documentList==null){
			documentList = JsonObject.create();
			documentList.put(LifeQuoteConstants.DOCID_CONFIG, serverConfig.getDocBYId(LifeQuoteConstants.DOCID_CONFIG).content());
			documentList.put(LifeQuoteConstants.RESPONSE_CONFIG_DOC, serverConfig.getDocBYId(LifeQuoteConstants.RESPONSE_CONFIG_DOC).content());
			return documentList;
		}else{
			return documentList;
		}
	}*/
}